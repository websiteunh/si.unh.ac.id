# Environment files
.env
.env.local
.env.*.local
# env.example - keep this as template

# Composer
/vendor/
composer.phar
# Note: composer.lock should be committed for version consistency

# NPM
node_modules/
package-lock.json
yarn.lock
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# User uploaded files (should not be in git)
/assets/uploads/
# Old symlink folders (no longer used, but ignore just in case)
/assets/art1kel/
/assets/im493/
/assets/dokum3nt/
/captcha/

# Application logs and cache
/application/logs/*.log
/application/logs/*.php
/application/cache/*
!/application/logs/index.html
!/application/cache/index.html

# System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo
*~
.project
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Temporary files
*.tmp
*.temp
*.bak
*.backup
*.old

# Excel files (generated)
/excel/*.xlsx
!/excel/.gitkeep

# IDE files
*.iml
.idea/
*.sublime-*

# OS files (duplicate prevention)
.AppleDouble
.LSOverride
Icon

# Jekyll (not used in this project)
_config.yml

# Compiled assets (if using build tools)
/assets/css/*.css.map
/assets/js/*.js.map

# Vendor assets (npm packages)
# Keep assets/vendor/ in git since hosting doesn't have npm
# /assets/vendor/ - NOT ignored, must be committed

# Keep these files/directories
!.gitkeep
!index.html
